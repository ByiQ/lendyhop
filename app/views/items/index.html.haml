-# in app/views/items/index.html.haml

%h3
  = link_to 'Create Listing', new_item_path
    
%h2 Available Items

- @items.each do |item|
  %div.listing
    %p= item.title
    %p= item.description
    %p= item.condition
    %p= item.status
    %p= link_to "More about #{item.title}", item_path(item)