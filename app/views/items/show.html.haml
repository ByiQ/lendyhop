-# in app/views/users/show.html.haml

%h2 Details about #{@item.title}

%h3 #{@item.title} - #{@item.price}
%p
    = @item.description
    %br
    %br
    = @item.condition
    %br
    = @item.status
    %br
    = @item.location

- if (session[:user] != nil && session[:user]["id"] == @item.user["id"])
    = link_to 'Edit', edit_item_path(@item)
    
= link_to 'Back to listings', items_path

= form_tag new_comment_path(@comment), :method => :put do

    = label :item, :comment, 'Add Comment'
    = text_field :item, :comment
  
    = submit_tag 'Submit'